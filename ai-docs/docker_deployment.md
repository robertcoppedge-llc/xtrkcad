"# Docker Container Deployment Guide for xtrkcad\n\nThis guide details how to containerize the xtrkcad application using Docker, ensuring a consistent and reproducible environment for development and deployment.\n\n## 🐳 Prerequisites\n\nThe following tools must be installed locally:\n*   Docker Engine: [Latest Version]\n*   Git:\n\n## 📄 Step 1: Create the Dockerfile\n\nA `Dockerfile` needs to be placed in the root directory of the source code repository (`xtrkcad-source-5.3.1GA/`). This file defines the entire build environment.\n\n```dockerfile\n# Use a stable base image that supports C++ development\nFROM ubuntu:22.04\n\n# Set environment variables and non-interactive mode\nENV DEBIAN_FRONTEND=noninteractive\nWORKDIR /app\n\n# Install essential build tools and runtime libraries\nRUN apt-get update && apt-get install -y \\\n    build-essential \\ \
    cmake \\ \
    git \\\n    libcurl4-openssl-dev \\ \
    libboost-all-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\n# Copy the source code into the container\nCOPY xtrkcad-source-5.3.1GA /app/xtrkcad-source-5.3.1GA\nWORKDIR /app/xtrkcad-source-5.3.1GA\n\n# Build the application\nRUN cmake -S . -B build && make -C build \\\n\n# Clean up development tools (optional, for smaller images)\n# RUN apt-get purge -y build-essential libboost-all-dev ...\n\n# Define the default command to run the application\nENTRYPOINT ["/app/xtrkcad-source-5.3.1GA/usr/app/bin/xtrkcad_cli"]\nCMD ["--help"]\n```\n\n## 🚀 Step 2: Build the Docker Image\n\nRun the following command from the directory containing the `Dockerfile`:\n\n```bash\ndocker build -t xtrkcad:latest . \n# The '.' indicates that the Dockerfile is in the current directory.\n```\n\nThis process will take time as it downloads dependencies, compiles the code (CMake/Make), and creates a layered image.\n\n## ▶️ Step 3: Run the Container\n\nYou can now run the application using the built image. Pass any necessary arguments after the image name:\n\n```bash\ndocker run -it xtrkcad:latest --config /path/to/my_settings.json \n# Example usage with a custom configuration argument.\n```\n\n### 💡 Troubleshooting & Optimization\n*   **Build Failure:** If compilation fails, ensure all necessary headers and development packages (like those listed in `apt-get install`) are present in the Dockerfile's base image. \n*   **Image Size:** For production deployment where security is paramount and rebuilding dependencies repeatedly is costly, consider using a multi-stage build pattern to separate build tools from the final runtime environment.\n\n---\n*Document generated on: YYYY-MM-DD.*"